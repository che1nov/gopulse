# demo-service

Small Go service used to test `gopulse` on a real benchmark target.

```bash
cd examples/demo-service
gopulse doctor
gopulse run
gopulse baseline save
gopulse check
gopulse report --format markdown
```
